class Sess
  ###
  /**
   * @constructor
  ###
  constructor : () ->

  middleware : () ->
    (req, res, next) =>
      res.statusCode = 200
      res.end JSON.stringify(req.session)

module.exports = () ->
  new Sess()